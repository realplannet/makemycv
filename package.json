{
  "name": "makemycv",
  "version": "2.0.0",
  "description": "MakeMyCV — Cloudflare Pages + D1 (fully off Vercel/Supabase)",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-flag=nodejs_compat",
    "deploy": "wrangler pages deploy public --project-name=makemycv",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.3",
    "docx": "^9.7.1",
    "mammoth": "^1.12.0",
    "razorpay": "^2.9.8",
    "resend": "^3.5.0",
    "uuid": "^11.1.1"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
